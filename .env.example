# Database Configuration
# PostgreSQL connection string (works with Neon, Railway, Supabase, or local PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA&sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/finloom?schema=public&sslmode=require"

# JWT Secret
# Generate a secure random string for production: openssl rand -base64 32
# This is used to sign JWT tokens for authentication
JWT_SECRET="your-secret-jwt-key-change-this-in-production"

# Node Environment
# Use "development" for local development, "production" for deployment
NODE_ENV="development"

# Optional: Next.js Configuration
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Prisma Configuration
# PRISMA_CLI_QUERY_ENGINE_TYPE="binary"
